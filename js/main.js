// 'use strict';

// console.log('test');

$(document).ready(function() {
    $('.review-slider').slick({
        arrows: true,
        prevArrow: ".icon-arrow-left",
        nextArrow: ".icon-arrow-right",
    });
});